[tool.poetry]
name = "matrix-project"
version = "0.1.0"
description = "Matrix loading programs exercise"
authors = ["izouriqi"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "*"
numpy = "*"
matplotlib = "*"
requests = "*"